From c300e9216b61364e6f9b550c90a2ae2c2271d416 Mon Sep 17 00:00:00 2001
From: Stian Soiland-Reyes <stain@apache.org>
Date: Thu, 16 Mar 2017 15:47:54 +0000
Subject: [PATCH 3/7] tick for selected

---
 src/index_view.m   | 4 ++--
 src/thread_pager.m | 6 +++---
 2 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/src/index_view.m b/src/index_view.m
index bd20e61..bae69f9 100644
--- a/src/index_view.m
+++ b/src/index_view.m
@@ -1767,10 +1767,10 @@ draw_index_line(IAttrs, Panel, Line, _LineNr, IsCursor, !IO) :-
 
     (
         Selected = selected,
-        mattr_draw(Panel, unless(IsCursor, Attrs ^ selected), "*", !IO)
+        mattr_draw(Panel, unless(IsCursor, Attrs ^ selected), "✓ ", !IO)
     ;
         Selected = not_selected,
-        draw(Panel, " ", !IO)
+        draw(Panel, "  ", !IO)
     ),
     mattr(Panel, unless(IsCursor, Attrs ^ standard_tag), !IO),
 
diff --git a/src/thread_pager.m b/src/thread_pager.m
index 74900a7..1c56b43 100644
--- a/src/thread_pager.m
+++ b/src/thread_pager.m
@@ -2832,7 +2832,7 @@ draw_sep(Attrs, Cols, MaybeSepPanel, !IO) :-
         MaybeSepPanel = yes(Panel),
         panel.erase(Panel, !IO),
         attr(Panel, Attrs ^ t_status ^ bar, !IO),
-        hline(Panel, char.to_int('-'), Cols, !IO)
+        hline(Panel, char.to_int('―'), Cols, !IO)
     ;
         MaybeSepPanel = no
     ).
@@ -2856,10 +2856,10 @@ draw_thread_line(TAttrs, Panel, Line, _LineNr, IsCursor, !IO) :-
 
     (
         Selected = selected,
-        mattr_draw(Panel, unless(IsCursor, Attrs ^ selected), "*", !IO)
+        mattr_draw(Panel, unless(IsCursor, Attrs ^ selected), " ✓", !IO)
     ;
         Selected = not_selected,
-        draw(Panel, " ", !IO)
+        draw(Panel, "  ", !IO)
     ),
     mattr(Panel, unless(IsCursor, Attrs ^ standard_tag), !IO),
 
-- 
2.20.1

